
if(PKG_CONFIG_FOUND)
  pkg_search_module(LIBMICROHTTPD "libmicrohttpd >= 0.9.37")
else()
  if(OS_WINDOWS AND LIBMICROHTTPD_INCLUDE_DIRS AND LIBMICROHTTPD_LIBRARIES)
    set(LIBMICROHTTPD_FOUND TRUE)
    set(WSOCK32_LIBS
      wsock32
      ws2_32)
  endif()
endif()

if(FORCE_CLIB_LIBMICROHTTPD)
  set(LIBMICROHTTPD_FOUND TRUE)
  set(LIBMICROHTTPD_INCLUDE_DIRS "")
  set(LIBMICROHTTPD_LIBRARIES "microhttpd")
endif()

set(CONF_COMPONENTS
  "src/clib/libs/libsys_cll")
set(CONF_HEADER_DIRS)

if(LIBMICROHTTPD_FOUND)
  set(CONF_DO_BUILD TRUE)
  set(CONF_INCLUDE_DIRS
      ${LIBMICROHTTPD_INCLUDE_DIRS})
  set(CONF_COMP_DEFS)
  set(CONF_LINK_LIBS
      ${LIBMICROHTTPD_LIBRARIES}
      ${WSOCK32_LIBS})
endif()

